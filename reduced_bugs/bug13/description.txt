error in reasoners

--> probably triggert by same error already reported in bug6_openllet

calling hermit and openllet leads to different results i.e. Openllet does not infer that ontology is inconsistent

call:
OWLOntologyDocumentSource source = new FileDocumentSource(ontFile, new FunctionalSyntaxDocumentFormat());
OWLOntologyManager manager = OWLManager.createOWLOntologyManager();
ont = manager.loadOntologyFromOntologyDocument(source);
OWLReasoner openllet = OpenlletReasonerFactory.getInstance().createReasoner(ont);

openllet.isConsistent()


Hermit infers inconsistency



grammar version: functional_OWL_EL.bnf  with functional_owl_el.isla with script generator_OWL_EL.py

found on 14.11.2024


reported:

